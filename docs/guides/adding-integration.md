# Adding an Integration

## When to Use
Connecting a new external service end-to-end (client + tools + admin panel + config).

<!-- Doc-keeper: populate from CLAUDE.md integration client pattern + overseerr_client.py canonical example -->
