# Maintaining the Database

## When to Use
Schema changes, new tables, connection pool updates, credential store modifications.

<!-- Doc-keeper: populate from schema init pattern, connection pool, migration approach -->
