"""Agent package for RadBot.

Assembly is manifest-driven via :mod:`radbot.agent.assembly`. The top-level
``agent.py`` proxy at the repo root is the public entry to the cached
``Assembly`` after :func:`radbot.agent.assembly.build_default_assembly` runs
during web startup.
"""
