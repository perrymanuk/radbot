# Docker Compose environment variables for radbot
# Copy this file to .env and fill in the required values:
#   cp .env.docker .env
#
# Then start the stack:
#   docker compose up -d

# ── Required ─────────────────────────────────────────────────────────
# Fernet encryption key for the credential store.
# Generate one with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
RADBOT_CREDENTIAL_KEY=

# Admin API bearer token (used to authenticate with /admin/)
RADBOT_ADMIN_TOKEN=changeme

# ── Optional: Google API ─────────────────────────────────────────────
# You can set this here OR configure it later via the Admin UI at /admin/
# GOOGLE_API_KEY=

# ── Optional: PostgreSQL ─────────────────────────────────────────────
# Defaults are fine for local dev; change if you have conflicts
# POSTGRES_USER=radbot
# POSTGRES_PASSWORD=radbot
# POSTGRES_DB=radbot

# ── Optional: Exposed ports ──────────────────────────────────────────
# Change these if the defaults conflict with local services
# POSTGRES_EXPOSED_PORT=5433
# QDRANT_EXPOSED_PORT=6334
# RADBOT_EXPOSED_PORT=8000

# ── Optional: Logging ────────────────────────────────────────────────
# LOG_LEVEL=INFO
